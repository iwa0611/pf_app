<div class="form">
  <h1>投稿ページ</h1>
  <%= form_with url: '/hobbyspot/posts' do |f| %>
    <%= f.hidden_field :user_id, value: "#{current_hobbyspot_user.id}" %>
    <%= f.hidden_field :user_name, value: "#{@user.name}" %>
    <div>
      <%= f.label :body, '内容' %>
      <%= f.text_field :body %>
    </div>
    <div>
      <%= f.label :image, "画像" %>
      <%= f.file_field :image %> 
    </div>
    <div>
      <%= f.submit '投稿' %>
    </div>
    <% end %>
</div>

